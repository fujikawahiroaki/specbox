json.extract! specimen_label, :id, :name, :data_label_flag, :coll_label_flag, :det_label_flag, :note_label_flag, :user_id, :created_at, :updated_at
json.url specimen_label_url(specimen_label, format: :json)
