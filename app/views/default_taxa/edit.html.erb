<div class="mx-auto md:w-2/3 w-full">
  <h1 class="font-bold text-4xl">Editing default taxon</h1>

  <%= render "form", default_taxon: @default_taxon %>

  <%= link_to "Show this default taxon", @default_taxon, class: "ml-2 rounded-lg py-3 px-5 bg-gray-100 inline-block font-medium" %>
  <%= link_to "Back to default taxa", default_taxa_path, class: "ml-2 rounded-lg py-3 px-5 bg-gray-100 inline-block font-medium" %>
</div>
