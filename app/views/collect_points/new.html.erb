<%= turbo_frame_tag "content-zone" do %>
  <div class="px-4 md:px-10 lg:px-20 py-4 max-w-screen-xl mx-auto">
    <h1 class="font-bold text-lg">採集地点の作成</h1>

    <%= render "form", collect_point: @collect_point, create_mode: true %>

    <%= link_to "一覧に戻る", collect_points_url_with_ranmemory, class: "mt-2 ml-2 normal-button px-2 py-1 inline-block", data: { turbo_frame: "content-zone", turbo_action: "advance" }
    %>
  </div>
<% end %>
